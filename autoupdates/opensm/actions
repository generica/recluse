#!/bin/bash

source /usr/local/recluse/config

rpm -Uvh --force /usr/local/recluse/centos/$centosversion/ofed/opensm-latest/*.rpm

chkconfig --add opensmd
chkconfig --level 345 opensmd on

/etc/init.d/opensmd restart
